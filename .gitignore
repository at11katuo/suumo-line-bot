# 生成されたキャッシュ（国交省データ由来。再配布リスク回避のため追跡しない）
cache/
# .gitkeep だけは例外として追跡する
!cache/.gitkeep

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# 評価結果DB（個人データのためコミットしない。バイナリなので差分も取れない）
*.db

# 環境変数ファイル（APIキーを含むため絶対にコミットしない）
.env
.env.*
